# LLM Provider Configuration
# use `azureopenai` for AzureOpenAI models, use 'openai' for OpenAI models, 'ollama' for local models
LLM_PROVIDER=ollama

# Temperature for the model (0-1)
MODEL_TEMPERATURE=0

# Reasoning Configuration
# Set to 'true' to disable thinking and reasoning steps in agent responses
# Accepted values: 'true', '1', 'yes' (case insensitive) to enable, anything else to disable
DISABLE_REASONING=false

# OpenAI or AzureOpenAI Configuration (used when LLM_PROVIDER=openai)
OPENAI_API_KEY=<YOUR OPENAI_API KEY>
OPENAI_MODEL=openai/gpt-4.1
OPENAI_BASE_URL=https://models.github.ai/inference
OPENAI_API_VERSION=2024-12-01-preview
OPENAI_DEPLOYMENT_NAME=gpt-4o-classification

# Ollama Configuration (used when LLM_PROVIDER=ollama)
OLLAMA_MODEL=qwen3
OLLAMA_BASE_URL=http://localhost:11434

# MCP Server URLs
GRAPH_MCP_SERVER_URL=http://localhost:8002/mcp
GITHUB_MCP_SERVER_URL=http://localhost:8080/sse
AZURE_MCP_SERVER_URL=http://localhost:5008/sse
CAAS_MCP_SERVER_URL=http://localhost:8003/mcp

# Debugging
GLOBAL_DEBUG=true

# Database Configuration
# Set to 'basic' for username/password authentication
# Set to 'service_principal' for Azure service principal authentication
DB_AUTH_METHOD=basic
# Azure Service Principal Configuration (only required when DB_AUTH_METHOD=service_principal)
# These are used for Azure Database for PostgreSQL with Azure AD authentication
ARM_CLIENT_ID=your-azure-client-id
ARM_CLIENT_SECRET=your-azure-client-secret
ARM_TENANT_ID=your-azure-tenant-id
JDBC_URL="postgresql://username:password@localhost:5432/username?sslmode=disable"
VECTOR_TABLE_NAME=document_embeddings
EMBEDDING_DIM=1024
# openai text-embedding-ada-002 is 1536
# EMBEDDING_DIM=1536
CHUNK_SIZE=512
CHUNK_OVERLAP=50

# Embedding Configuration
EMBEDDING_MODEL=mxbai-embed-large
EMBEDDING_BASE_URL=http://localhost:11434
EMBEDDING_API_KEY=
EMBEDDING_API_VERSION=2023-05-15
EMBEDDING_DEPLOYMENT_NAME=text-embedding-ada-002

# Ping Federate
PF_JWKS=<Ping Federate JWKS URL>
PF_ISSUER=<Ping Federate Issuer URL>
PF_AUDIENCE=<Pingf Federate Client ID>

# Other Configurations
ENCRYPTION_SECRET_KEY=
LANGSMITH_API_KEY=